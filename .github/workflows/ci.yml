name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2-beta
        with:
          node-version: '16.8'
      - run: yarn install
      - run: yarn typecheck
      - run: yarn vite:build
      - run: yarn utility:build
      - run: yarn test
      - run: yarn lint

